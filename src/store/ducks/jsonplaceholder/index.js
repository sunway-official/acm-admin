import reducer from './reducers';

export { default as jsonplaceholderTypes } from './types';
export { default as jsonplaceholderActions } from './actions';
export { default as jsonplaceholderOperations } from './operations';

export default reducer;
