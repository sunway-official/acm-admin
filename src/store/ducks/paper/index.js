import reducer from './reducers';

export { default as paperTypes } from './types';
export { default as paperActions } from './actions';
export { default as paperOperations } from './operations';

export default reducer;
